# 🏭 Test For Lab - Система управления резервуарами

Панель администратора для управления резервуарами и продуктами на производстве. Реализован полный CRUD-функционал с валидацией данных и адаптивным дизайном.

## 🚀 Демо

**Live Demo:** [test-for-lab.vercel.app](https://test-for-lab.vercel.app)

## 📋 Содержание

- [Особенности](#особенности)
- [Технологии](#технологии)
- [Установка](#установка-и-запуск)
- [Использование](#использование)
- [API](#api)
- [Структура проекта](#структура-проекта)
- [CI/CD](#ci-cd)
- [Разработка](#разработка)

## ✨ Особенности

### 🗂️ Управление резервуарами
- **Просмотр списка** - отображение всех резервуаров с информацией о заполненности
- **Добавление** - создание новых резервуаров с валидацией
- **Редактирование** - изменение параметров существующих резервуаров
- **Удаление** - безопасное удаление резервуаров
- **Блокировка/разблокировка** - управление статусом доступности резервуара

### 🔍 Дополнительные возможности
- **Поиск по имени** - быстрый поиск резервуаров
- **Валидация форм** - проверка корректности введенных данных
- **Адаптивный дизайн** - корректное отображение на всех устройствах
- **Модальные окна** - удобный интерфейс для операций CRUD

## 🛠️ Технологии

### Frontend Stack
- **React 19** - библиотека для создания пользовательских интерфейсов
- **Next.js 15** - full-stack React фреймворк с SSR/SSG
- **TypeScript** - статическая типизация для JavaScript
- **Zustand** - легковесное управление состоянием

### Стилизация
- **SCSS** - препроцессор CSS
- **CSS Modules** - локальная область видимости стилей

### Формы и валидация
- **React Hook Form** - производительная библиотека форм
- **Zod** - TypeScript-first схема валидации

### Инструменты разработки
- **ESLint** - статический анализ кода
- **Prettier** - форматирование кода
- **GitHub Actions** - CI/CD pipeline
- **Turbopack** - быстрый bundler от Vercel

## 📦 Установка и запуск

### Предварительные требования
- Node.js 18.x или выше
- npm или yarn

### Установка

1. **Клонирование репозитория:**
```bash
git clone https://github.com/Max-Sam231/test-for-lab.git
cd test-for-lab
```

2. **Установка зависимостей:**
```bash
npm install
```

3. **Настройка переменных окружения:**
```bash
# Создайте файл .env.local
NEXT_PUBLIC_API_URL=https://reservoir-api.caravanlabs.ru
```

4. **Запуск в режиме разработки:**
```bash
npm run dev
```

5. **Откройте в браузере:**
   - http://localhost:3000

## 💻 Использование

### Основные операции

**Просмотр резервуаров:**
- На главной странице отображается список всех резервуаров
- Показывается информация: название, вместимость, текущий объем, продукт, статус

**Добавление резервуара:**
1. Нажмите кнопку "Добавить резервуар"
2. Заполните форму:
   - Название резервуара
   - Вместимость (л)
   - Текущий объем (л)
   - Выберите продукт
3. Сохраните изменения

**Редактирование:**
1. Нажмите на резервуар или кнопку редактирования
2. Измените необходимые поля
3. Сохраните изменения

**Поиск:**
- Используйте поле поиска для фильтрации по названию

## 🌐 API

### Endpoints

| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/reservoirs/` | Получить список всех резервуаров |
| GET | `/reservoirs/{id}` | Получить резервуар по ID |
| POST | `/reservoirs/` | Создать новый резервуар |
| PUT | `/reservoirs/{id}` | Обновить резервуар |
| DELETE | `/reservoirs/{id}` | Удалить резервуар |
| PATCH | `/reservoirs/{id}/toggle-lock` | Заблокировать/разблокировать |
| GET | `/products/` | Получить список продуктов |

### Модели данных

**Reservoir:**
```typescript
type Reservoir = {
  id: number;
  name: string;
  capacity: number;  // вместимость
  volume: number;    // текущий объем
  productId: number;
  isLocked: boolean;
  product: {
    id: number;
    name: string;
  };
};
```

## 📁 Структура проекта

```
src/
├── api/                  # API функции
│   └── reservoirs.ts     # CRUD операции с резервуарами
├── components/           # React компоненты
│   ├── common/          # Общие компоненты
│   ├── forms/           # Компоненты форм
│   └── modals/          # Модальные окна
├── schemas/             # Схемы валидации
│   └── ValidationScheme.tsx
├── store/               # Zustand хранилища
├── styles/              # SCSS стили
├── types/               # TypeScript типы
│   └── types.ts
└── pages/               # Next.js страницы

public/
└── assets/              # Статические файлы
    └── svg/             # SVG иконки
```

## 🔄 CI/CD

### GitHub Actions

Проект использует автоматизированный пайплайн для проверки качества кода:

```yaml
# .github/workflows/ci.yaml
- Проверка форматирования (Prettier)
- Статический анализ (ESLint)
- Автоматический запуск при push/PR
```

**Запуск локально:**
```bash
# Проверка форматирования
npm run check-format

# Форматирование кода
npm run format

# Запуск линтера
npm run lint
```

## 📋 Доступные скрипты

| Команда | Описание |
|---------|----------|
| `npm run dev` | Запуск в режиме разработки с Turbopack |
| `npm run build` | Сборка для production |
| `npm run start` | Запуск production сервера |
| `npm run lint` | Проверка кода ESLint |
| `npm run format` | Форматирование кода Prettier |
| `npm run check-format` | Проверка форматирования |

## 🧪 Разработка

### Валидация форм

Проект использует Zod для валидации:

```typescript
// Схема валидации резервуара
export const reservoirSchema = z.object({
  name: z.string().nonempty("Название обязательно"),
  capacity: z.number().positive("Вместимость должна быть > 0"),
  volume: z.number().min(0, "Объем должен быть >= 0"),
  productId: z.number().positive("Выберите продукт")
}).superRefine(({ capacity, volume }, ctx) => {
  if (volume > capacity) {
    ctx.addIssue({
      message: "Объем не может превышать вместимость",
      path: ["volume"]
    });
  }
});
```

### Управление состоянием

Используется Zustand для простого и эффективного управления состоянием:

```typescript
// Пример store
const useReservoirStore = create((set, get) => ({
  reservoirs: [],
  loading: false,
  // actions
}));
```

## 🤝 Вклад в проект

1. Fork проекта
2. Создайте feature branch (`git checkout -b feature/amazing-feature`)
3. Commit изменения (`git commit -m 'feat: add amazing feature'`)
4. Push в branch (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## 📄 Лицензия

Этот проект создан в учебных целях и для демонстрации навыков разработки.

## 👨‍💻 Автор

**Максим Марютин** - [Max-Sam231](https://github.com/Max-Sam231)

---

⭐ Поставьте звезду, если проект был полезен!